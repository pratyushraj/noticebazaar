# Skip optional dependencies during install
# This prevents canvas from being installed on Vercel
# pdfjs-dist will work without canvas (it's only needed for canvas rendering)
optional=false

# Skip Puppeteer Chrome download during npm install
# Chrome will be downloaded on-demand at runtime if needed, or use system Chrome
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true

